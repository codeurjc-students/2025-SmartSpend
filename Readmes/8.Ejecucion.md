## 🚀 Ejecución de la Aplicación

### Requisitos Previos

Para ejecutar Smart Spend necesitas tener Docker instalado en tu sistema:

#### Windows y Mac
- **Docker Desktop**: [Descargar Docker Desktop](https://www.docker.com/products/docker-desktop/)
- Incluye Docker y Docker Compose automáticamente

#### Linux
- **Docker Engine**: [Instalar Docker en Linux](https://docs.docker.com/engine/install/)
- **Docker Compose**: [Instalar Docker Compose](https://docs.docker.com/compose/install/)

### Ejecución con Docker Compose

1. **Clona el repositorio** (si no lo tienes ya):
```bash
git clone https://github.com/codeurjc-students/2025-SmartSpend.git
cd 2025-SmartSpend
```

2. **Navega al directorio docker**:
```bash
cd docker
```

3. **Ejecuta la aplicación**:
```bash
docker-compose up -d
```

4. **Accede a la aplicación**:
   - Abre tu navegador web
   - Ve a: `https://localhost:443`
   - **Nota**: Como usa certificados autofirmados, acepta el aviso de seguridad del navegador

### Comandos útiles

#### Levantar nueva imágen
```bash
docker build -t juanmabustos/smartspend:dev -f docker/Dockerfile .
```
#### Levantar contenedor 
```bash
docker-compose -f docker-compose-dev.yml up --build
```

#### Detener la aplicación:
```bash
docker-compose -f docker-compose-dev.yml down -v
```

#### Lanzar test de newman*:
```bash
docker-compose -f docker-compose-ci.yml up newman
```



#### Ver logs en tiempo real:
```bash
docker-compose logs -f
```

#### Reiniciar solo la aplicación:
```bash
docker-compose restart app
```

#### Limpiar datos y reiniciar desde cero:
```bash
docker-compose down -v
docker-compose up -d
```

---

## 🌐 Acceso a la Aplicación en Producción (Render)

### Version desplegada en produccion

Smart Spend está desplegado en **Render** y está disponible en:

🔗 **URL:** https://smartspend-dev.onrender.com

### Caracteristicas del despliegue en produccion

- **Infraestructura:** Render
- **Base de datos:** MySQL en **Aiven** (servicio remoto desacoplado)
- **Actualizaciones:** Gestionadas por el flujo de despliegue del proyecto
- **Certificado SSL:** Gestionado por la plataforma

### Diferencia entre Desarrollo y Producción

| Aspecto | Desarrollo Local | Producción (Render) |
|--------|-----------------|------------------|
| **URL** | `https://localhost:443` | `https://smartspend-dev.onrender.com` |
| **Entorno** | Docker Compose local | Servicio cloud en Render |
| **Base de datos** | MySQL en contenedor local | MySQL remota en Aiven (desacoplada) |
| **Scripts de test** | Disponibles (`docker-compose-ci.yml`) | Ejecutados en CI/CD |
| **Actualización** | Manual con `docker-compose up --build` | Pipeline de despliegue del proyecto |

Para más detalles sobre la arquitectura y el despliegue, consulta la [Guía de Desarrollo](7.GuiaDeDesarrollo.md#despliegue-en-produccion-render).

---

### Solución de Problemas

#### La aplicación no carga
- Verifica que Docker esté ejecutándose
- Comprueba que el puerto 8443 no esté ocupado
- Revisa los logs: `docker-compose logs app`

#### Error de certificado SSL
- En local, acepta el certificado autofirmado
- En Chrome/Edge: Haz clic en "Avanzado" → "Ir a localhost (no seguro)"
- En Firefox: Haz clic en "Avanzado" → "Aceptar el riesgo y continuar"
- **Nota:** El aviso suele darse en local; en producción el certificado lo gestiona Render

#### Base de datos no responde
- Espera unos minutos para que MySQL termine de inicializarse
- Verifica el estado: `docker-compose ps`
- Reinicia si es necesario: `docker-compose restart db`


---
