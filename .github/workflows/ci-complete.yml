name: CI - Complete Quality Check

on:
  pull_request:
    branches: [ "main" ]

env:
  SPRING_PROFILES_ACTIVE: prod
  # Define DOCKER_HUB_USERNAME as it's used in docker-compose-ci.yml for the app-ci image
  DOCKER_HUB_USERNAME: ${{ github.repository_owner }} # Or your specific Docker Hub username

jobs:
  backend-complete:
    runs-on: ubuntu-latest
    name: Backend Complete Quality Check
    env:
      KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
    steps:
      - uses: actions/checkout@v4

      # Creates the keystore file from a secret for integration tests.
      - name: Create Keystore from Secret for Backend
        env:
          KEYSTORE_P12_BASE64: ${{ secrets.KEYSTORE_P12_BASE64 }}
        run: |
          echo "Creating keystore file from secret for backend..."
          echo "${KEYSTORE_P12_BASE64}" | base64 --decode > backend/src/main/resources/keystore.p12
          echo "Keystore file created successfully."

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Build backend
        working-directory: backend
        run: mvn clean compile -B

      - name: Run backend unit tests
        working-directory: backend
        run: mvn test -Dtest="*Unit*,*ServiceTest*" -Dsurefire.failIfNoSpecifiedTests=false -B

      - name: Integration and E2E Tests Status
        run: |
          echo "🚧 Integration and E2E Tests Status 🚧"
          echo ""
          echo "The following tests are temporarily disabled while authentication functionality is completed:"
          echo "❌ Integration Tests (JVM-based, require JWT authentication)"
          echo "❌ E2E Selenium Tests (require full login flow)"
          echo ""
          echo "API Integration Tests (Postman/Newman) are now enabled and will run in a separate job using Docker Compose."
          echo "✅ Only backend unit tests are active in this job."

  postman-api-tests:
    runs-on: ubuntu-latest
    name: Postman API Tests (Newman with Docker Compose)
    needs: [backend-complete]
    env:
      # These are required by app-ci service in docker-compose-ci.yml
      JWT_SECRET_DEV: ${{ secrets.JWT_SECRET }} # Mapping to JWT_SECRET from GitHub Secrets
      KEYSTORE_PASSWORD_DEV: ${{ secrets.KEYSTORE_PASSWORD }} # Mapping to KEYSTORE_PASSWORD from GitHub Secrets
      DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }} 
    steps:
      - uses: actions/checkout@v4

      # Build the backend JAR and Docker image as required by app-ci service
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Create Keystore from Secret for Backend Docker Image
        env:
          KEYSTORE_P12_BASE64: ${{ secrets.KEYSTORE_P12_BASE64 }}
        run: |
          echo "Creating keystore file from secret for backend image build..."
          echo "${KEYSTORE_P12_BASE64}" | base64 --decode > backend/src/main/resources/keystore.p12
          echo "Keystore file created successfully."

      - name: Build and Package Backend
        working-directory: backend
        run: mvn clean package -DskipTests -B # package to create the JAR for Docker build

      - name: Build Backend Docker Image
        run: |
          docker build -t ${DOCKER_HUB_USERNAME}/smartspend:dev ./backend # Build the image expected by docker-compose

      # Run all services using docker-compose-ci.yml
      # The --exit-code-from newman will make the job fail if the newman service exits with a non-zero code.
      - name: Run Docker Compose Services and Newman Tests
        working-directory: docker # Assuming docker-compose-ci.yml is in the 'docker' folder
        run: |
          docker-compose -f docker-compose-ci.yml up --build --abort-on-container-exit --exit-code-from newman

      - name: Upload Newman Test Report
        uses: actions/upload-artifact@v4
        if: always() # Upload report even if tests fail
        with:
          name: newman-test-report
          # The report is generated by newman service in /workspace, which is mounted from ../postman/
          # So, it will be in the postman/ directory relative to the repository root.
          path: postman/newman-report.xml

      - name: Show Docker Compose Logs on Failure
        if: failure()
        run: |
          echo "--- Docker Compose Logs ---"
          # This assumes you have the logs from the previous step.
          # You might need to adjust based on how you capture logs in the `docker-compose up` step.
          # A simpler approach for debugging on failure might be to rerun with `docker-compose logs` after the failure.
          # For now, we'll keep it simple or remove if not easily available.
          echo "Refer to the 'Run Docker Compose Services and Newman Tests' step for detailed logs."

  quality-gate:
    runs-on: ubuntu-latest
    name: Quality Gate
    needs: [backend-complete, postman-api-tests]
    
    steps:
      - name: Quality Gate Passed
        run: |
          echo "✅ Quality Gate PASSED"
          echo ""
          echo "📋 Executed Tests Summary:"
          echo "  ✅ Backend Unit Tests: PASSED"
          echo "  ✅ Postman API Integration Tests: PASSED (via Docker Compose)"
          echo "  🚧 Backend Integration Tests (JVM-based): SKIPPED (temporary)"
          echo "  🚧 E2E Tests: SKIPPED (temporary)"
          echo ""
          echo "🚀 Ready to merge! Continue with feature development."